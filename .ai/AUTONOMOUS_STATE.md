# Autonomous State

- Current roadmap version: v3
- Current task ID: AUTO-230 — Stream and reverify confirmed archive-package writes
- Current task status: DONE
- Current branch: main
- Last run timestamp: 2026-08-29T23:12:00+04:00
- Latest run summary: `forge maintenance-archive-package` now streams tar/zip source evidence and final package SHA-256 computation in 64 KiB chunks. During confirmed package construction, every entry's expected byte count is rechecked against the reviewed package preview and entries with an expected SHA-256 are digest-verified on the exact bytes written before no-clobber publication. Same-size digest-bound source drift now fails closed before publication.
- Safety: Existing ready-preview gating, explicit `--confirm-package`, repository containment, same-directory temporary construction, fsync durability, no-clobber publication, and downstream package verification remain intact. Advisory entries that intentionally lack an upstream expected SHA remain advisory rather than being promoted into synthetic digest evidence. No new validation, Git, workflow, push, network, remote, or branch-protection authority was introduced.
- Repository assessment: Confirmed the AUTO-229 baseline was green, then inspected README/docs/examples, archive/package source and tests, config/CI, policy and `.ai` plan/state/changelog/decisions, recent commits, open issues, all eight visible branches, and PR history. Seven non-main branches remain historical/diverged; open issues are broader project requests; inspected PRs are merged, closed, obsolete, superseded, or unrelated. Nothing warranted integration.
- Branch and PR disposition: Work stayed directly on `main`; no branch or PR was created, merged, force-pushed, or substituted for the requested main-only workflow.
- Validation: The first AUTO-230 product head reached pytest on all supported versions but failed because the new digest gate incorrectly treated an intentionally absent advisory `run_history_link` SHA as a mismatch. The correction keeps expected-byte checks for all entries, enforces SHA only when the reviewed preview supplies one, and adds same-size digest-drift regression coverage. Corrected product head `8d3d1a034fe485dc52fb630d4ed708d3e2423264` passed Actions run `33270213540`; Python 3.10, 3.11, and 3.12 all passed package installation, source compilation, installed CLI smoke tests, roadmap validation, and pytest.
- Current blockers: None.
- Known risks and assumptions: Exact package creation and hashing still read every source byte, so runtime and disk I/O remain proportional to evidence size. Advisory entries without an upstream expected SHA can only be byte-count checked at this write boundary; inventing a digest here would change their evidence semantics rather than strengthen the existing contract.
- Visuals: None; package-write internals changed without changing maintenance/preservation workflow topology.
- Project-memory note: `docs/MAINTENANCE_ARCHIVE_PACKAGE.md`, focused AUTO-230 tests, README, this state file, and `.ai/AUTO-230.md` carry the detailed run record. `AUTONOMOUS_PLAN.md`, `AUTONOMOUS_CHANGELOG.md`, and `DECISIONS.md` were inspected; the roadmap milestone and architecture remain unchanged, so no artificial semantic rewrite was made.
- Recommended next task: Inspect whether the advisory archive entry can obtain a trustworthy digest at its authoritative upstream boundary without changing evidence semantics; otherwise continue to the next concrete preservation write/integrity defect. Any fresh CI failure takes priority.
