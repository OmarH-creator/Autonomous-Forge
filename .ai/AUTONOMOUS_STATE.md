# Autonomous State

- Current roadmap version: v3
- Current task ID: AUTO-142 — Restore green main baseline without weakening evidence contracts
- Current task status: TODO
- Current branch: main
- Last run timestamp: 2026-08-15T03:12:00+04:00
- Last successful implementation commit hash: `8160132ad188033d66a6290c180bbd718fe42952`
- Latest run summary: Continued issue #13 baseline recovery by repairing one shared archive-path defect across both archive-copy preview and archive-copy verification. Absolute-but-repository-contained manifest entry paths are now canonicalized to repository-relative paths after containment validation before they are joined beneath the requested archive root. This prevents pathlib absolute-path joins from discarding the archive-root prefix while retaining repository-root containment, archive-root containment, collision, overwrite, hash, and byte-count safeguards.
- Files changed in the latest run: `src/autonomous_forge/maintenance_archive_copy_preview.py`, `src/autonomous_forge/maintenance_archive_copy_verify.py`, README, and `.ai` project-memory records.
- Validation commands and results: Broad inspection covered README/docs/source/tests/config/CI, roadmap/state/changelog/decisions, issue #13, recent commits, all visible branches, and PR history. At cycle start the annotated Python 3.10/3.11/3.12 matrix reported 57 failed and 597 passed. Commit `bedb09e77ab672e8716ab1fc3910a06549afd478` fixed archive-copy preview mapping and reduced Python 3.11 to 51 failed and 603 passed. Commit `8160132ad188033d66a6290c180bbd718fe42952` applied the same canonical repository-relative mapping to archive-copy verification; its Python 3.11 run reached 23 failed and 631 passed, with the entire archive-copy/verify/package/preservation failure cluster absent. Install, compile, installed CLI smoke, and roadmap lint pass on Python 3.10, 3.11, and 3.12; pytest remains red on all three versions.
- Branch and PR assessment: Work stayed directly on `main`. Historical feature and maintenance branches remain stale or superseded; recent PRs are merged, closed, or obsolete, so nothing was merged and no replacement PR was created.
- Current blockers: `main` is still red with 23 failures in the inspected Python 3.11 run. Remaining clusters are one replay-policy primary-router help assertion, stale enriched planning/validation/executor output-contract assertions, and four maintenance-review-compare tests including three fixture directory-collision failures. The Python 3.10/3.11/3.12 matrix must all pass before feature delivery resumes.
- Known risks and assumptions: Canonicalization occurs only after proving the source path is inside the configured repository root. Destination containment under the selected archive root is still checked, existing destinations still block copy previews, and copied evidence is still verified by expected byte count and SHA-256. No overwrite, repository-root escape, archive-root escape, remote, workflow-rerun, commit, or push authority was added.
- Recommended next task: Continue AUTO-142 with the remaining deterministic red-baseline clusters, prioritizing the stale enriched planning/validation/executor output-contract assertions as the largest group, then the replay-policy help assertion and maintenance-review-compare fixture/output failures.